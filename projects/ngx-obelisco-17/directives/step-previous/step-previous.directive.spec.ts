import { StepPreviousDirective } from './step-previous.directive';

describe('StepPreviousDirective', () => {
  it('should create an instance', () => {
    const directive = new StepPreviousDirective();
    expect(directive).toBeTruthy();
  });
});
