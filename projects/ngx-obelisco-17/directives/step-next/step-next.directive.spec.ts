import { StepNextDirective } from './step-next.directive';

describe('StepNextDirective', () => {
  it('should create an instance', () => {
    const directive = new StepNextDirective();
    expect(directive).toBeTruthy();
  });
});
